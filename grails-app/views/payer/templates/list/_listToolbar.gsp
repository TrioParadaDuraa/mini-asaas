<atlas-toolbar>
    <atlas-button
            icon="plus"
            description="Cadastrar pagador"
            href="${createLink(controller: 'payer', action: 'index')}"
            slot="actions"
    ></atlas-button>
</atlas-toolbar>